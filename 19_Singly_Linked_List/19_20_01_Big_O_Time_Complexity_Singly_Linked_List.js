// Big O of singly Linked List 

// Insertion - O(1)

// Removal - It Depends --> O(1) OR O(N)
//    if u r removing form the begging its very easy  O(1)
//    If u r removing from the end its little difficult O(N)
        //   Beacuse we need to find the item right before the tail 
        //   and it to do that we need to iterate  the entire list 
                //  best case - O(1)
                //  worst case - O(n)

// Searching - O(N)

// Access - O(N)